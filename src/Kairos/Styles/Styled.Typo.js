import styled from '@emotion/styled';
import { Typography } from '@mui/material';



export const Fail = styled(Typography)(() => ({
    fontSize: 11,
    color: '#f381a7',
    display: 'inline',
    marginLeft: 6
  })); 

export const Smallfont = styled(Typography)(()=>({
    fontSize: 11,
    textAlign: 'center', 
    display: 'inline'
}))